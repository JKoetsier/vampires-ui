'use strict';

describe('myApp.config module', function() {

    beforeEach(module('myApp.config'));

    describe('config controller', function(){

        it('should ....', inject(function($controller) {
            //spec body
            var configCtrl = $controller('ConfigController');
            expect(configCtrl).toBeDefined();
        }));

    });
});